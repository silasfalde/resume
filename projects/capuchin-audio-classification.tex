\experience{Capuchin Audio Classification}{Ann Arbor, MI}{Course Project}{Machine Learning Developer}
\begin{bullets}
    \item \textbf{Built an end-to-end PyTorch pipeline} to detect capuchin monkey calls in forest recordings by loading, resampling, and batching variable-length audio.
    \item \textbf{Converted recordings to padded spectrogram tensors} and trained a convolutional neural network with GPU support, checkpointing, and TensorBoard loss tracking.
    \item \textbf{Ran inference} over field recordings and exported per-recording call counts to CSV for downstream analysis.
\end{bullets}